# GPT Baselines

Contains the preliminary pure baselines:

- GPT-4o mini scenario-only predictions
- GPT-4o mini context-appended baseline runner
- DeBERTa MNLI preliminary baseline

The original Colab notebook is kept in `notebooks/` for provenance. The reusable script is in `scripts/`, and baseline outputs are in `results/`.
